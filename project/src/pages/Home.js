import BigTitle from '../components/BigTitle';


const Home = () => {
    return (
      <div>
        <BigTitle title="Choose a (something related to API)"/>
      </div>
    );
  };
  
  export default Home;
  